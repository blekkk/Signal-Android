package org.thoughtcrime.securesms.components;

import android.annotation.SuppressLint;
import android.app.Activity;
import android.os.Bundle;
import android.view.View;
import android.widget.ImageButton;
import android.widget.Toast;

import org.thoughtcrime.securesms.R;

public class alexCats extends Activity {

/*    @SuppressLint("ResourceType")
    @Override
    protected void onCreate(Bundle savedInstance) {
        super.onCreate(savedInstance);
        setContentView(R.style.Signal_DayNight);


        ImageButton test = (ImageButton) findViewById(R.id.toLight_button);

        test.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Toast.makeText(getApplicationContext(), "Asu kampank", Toast.LENGTH_LONG).show();
            }
        });
    }*/
}
